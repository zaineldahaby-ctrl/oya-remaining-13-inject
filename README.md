# OYA · Remaining-13 · injection (P05 command/global)

Public injection repo for Figma import (GitHub Pages). Mirrors the established `-inject` convention
(flat board HTML at root, served via Pages). Editable Figma masters are built from these boards.

- 22 boards (11 ar + 11 en), source-faithful, extracted from the canonical runtime.
- Private source/handoff: `oya-remaining-13-handoff` (tag `p05-command-global-v1`).
